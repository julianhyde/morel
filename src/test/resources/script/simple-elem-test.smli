(*
 * Simple ELEM pattern test for Phase 0 spike validation
 * Tests FunctionRegistry infrastructure with basic invertible pattern
 *)

(* Define edges collection *)
val edges = [(1,2), (2,3)];
> val edges = [(1,2),(2,3)] : (int * int) list

(* Simple edge predicate - should be invertible via FunctionRegistry *)
fun edge (x, y) = (x, y) elem edges;
> val edge = fn : int * int -> bool

(* Query using predicate inversion *)
from p where edge p;
> val it = [(1,2),(2,3)] : (int * int) bag

(*) End simple-elem-test.smli
